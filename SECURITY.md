# 安全与隐私

## 运行边界

本项目提供监控，不负责 NAS 的断电保护。关机、低电量策略和设备控制继续由系统 UPS 服务管理。功率模型和私有字段中的候选解释不能用作唯一的关机或电源安全依据。

采集器通过宿主机 usbmon 读取 USB 通信，不向 UPS 发出控制命令、不占用其 USB 接口。usbmon 观测范围是所在总线，应用层才筛选目标设备，因此其权限不能描述成严格限于一个 UPS。不要将 `/dev/usbmon*` 改为所有用户可读，也不要给页面容器挂载 USB 总线。

systemd 采集器以受限制的 root 身份运行；模块加载是宿主机级操作。安装脚本和源码应来自你信任的版本。Docker 镜像固定使用普通用户运行，Compose 只读挂载采集目录。

## 网络访问

页面与 API **没有内置身份验证或 TLS**。默认通过 NAS 的 `9086` 端口提供局域网访问，能访问该端口的人可读取遥测、历史和诊断，也可通过校准 API 改变后续功率估算的配置。需要限制监听地址时，直接将 Compose 的端口改为 `NAS局域网IP:9086:8080`，或使用 `127.0.0.1:9086:8080` 配合本机反向代理。公网或不受信任网络需要额外认证、TLS 与访问控制，不应直接做路由端口转发。

网页校准仅将受校验的配置写入已有 `data/calibration.json`，宿主机采集器读取后用于经验估算。它不修改原始遥测、已有历史、NAS 关机策略或 UPS 控制设置。自定义系数的范围只约束输入，不证明精度；保存成功也不等于采集器已生效。

校准写入要求 JSON 与专用请求头，并检查浏览器来源；API 不开放跨域写入。反向代理应保留浏览器访问使用的 `Host`，不要额外放开跨域权限。

静态资源随应用打包，没有统计上报或远程 CDN 运行依赖。硬件来源链接在用户打开时才访问第三方网站。诊断、备份与截图仍可能包含设备标识和使用情况，分享前需检查。

## 报告问题

漏洞报告请优先使用仓库 Security 页中的私密报告入口（若已启用）。若未启用，可提交不含利用细节与私人数据的问题，申请一个私密沟通渠道；不要在公开 Issue 中贴出令牌、序列号、完整日志或可利用敏感信息。

报告时包含受影响版本、最小复现条件、影响范围及可行的修复建议。当前维护重点为最新版本；旧版是否受影响需要单独确认。

## 发布与备份

运行数据库、真实环境配置、个人校准记录和原始抓包不属于公开发布内容。`.gitignore` 只能忽略尚未跟踪的文件，不能清除已经写入 Git 历史的秘密。若凭据曾被提交，应立即撤销或轮换，并处理历史副本；仅删掉当前文件不够。

数据库升级前使用 SQLite 备份接口，并单独保存已有的 `data/calibration.json`，敏感备份仅存放在受控位置。恢复操作先停止写入进程；不要向未知来源提供整个生产数据卷来诊断一个显示问题。
