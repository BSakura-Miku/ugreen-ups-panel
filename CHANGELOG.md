# 更新记录

## 0.7.0 — 电池观察参考（2026-09-07）

- 新增稳定待机后的压差参考分级与建议，连续观察 30 分钟、区间持续 2 分钟后确认；充放电与数据失鲜时不借用待机等级。
- 电芯卡显示本次待机近期峰值与最常单独偏低电芯，支持跳转 7 天、30 天压差趋势。
- 电池供电记录增加观测范围内的估算放电量、有效覆盖时长/比例及功率依据；按未平滑采样积分，缺口和切换边界不补算。
- 保留旧历史与供电明细，旧记录不回填 Wh；未配置电池倍率时不套样机默认值。能量与游标随历史事务保存，断档或依据改变后不跨段累计。
- 两项均为观察参考，不提供电池容量、SOH 或故障诊断。新增数据旁表兼容旧面板，已有 v0.6.0 采集器无需重复安装。

## 部署配置更新（2026-09-07）

- 默认 Compose 增加 `user: "0:0"`，兼容部分 NAS 的绑定数据目录权限，处理因此导致的 SQLite 无法打开数据库问题；镜像默认普通用户保持不变。
- 补充 root 运行的原因、权限边界及非 root 替代方案，包括保留历史与校准数据的所有者调整和容器重建步骤。
- 本次仅更新部署配置与说明，已有容器需应用 Compose 修改并重建；只拉取镜像或重启不会改变其运行用户。

## 0.6.0 — 手工读数分步校准（2026-09-07）

- 新增交流读数分步助手：确认适配器输入电压，在线且未充电时测交流基底，回充补偿可在后续充电时补做。根据输入侧读数预选 12/19/20 V，始终要求用户确认。
- 每步采样 30 秒、至少 12 个去重读数；相关原值波动超过 10%、数据缺失或状态变化时重采。充电功率原值均值低于 2 W 时不计算回充系数。
- 新版自定义配置可只填写 `base_gain`，未配置的 `charge_gain`、`battery_gain` 保存为 `null`。已有其他系数保留，更换适配器电压需重测交流基底和回充补偿。
- 校准 schema 2 为 `custom` 增加 12/19/20 V 标称适配器电压，运行时按 ±1 V 匹配；schema 1 原哈希和 18–20 V 规则保持。12 V 不提供样机预设，也未宣称完成实机精度验证。
- 接口增加配置版本能力声明，新页面识别采集器支持情况；旧页面不能覆盖新版配置。电压纳入校准版本与历史身份，不混算不同配置。
- **本次需要先升级宿主机采集器，再更新面板容器。** 保留现有数据与配置；旧采集器不支持 schema 2，回滚需恢复兼容校准配置，不能靠删除历史解决。
- 助手仅使用用户手工填写的交流读数，不自动对接 HA、控制智能插座或执行断电。NAS 原有 UPS 保护服务继续负责关机与保护。

## 0.5.0 — 长期趋势与电池供电记录（2026-09-07）

- 趋势新增 30 天、90 天、半年（180 天）和一年（365 天），完整显示所选时间范围；实际记录日期单独标明，未采集与中断时段留空。
- 保留原有 10 秒/7 天、分钟/90 天统计，新增按 UTC 日期划分的日统计，保留 365 天，用于超过 90 天的查询。
- 从数据库中仍保留的旧历史补建日统计；不会恢复已删除记录或生成过去未采集的数据。统计继续保留总和、计数、最小值与最大值，不混合供电模式或校准配置。
- 电芯压差同时显示均值与峰值，CSV 增加极值列，便于对照长期变化与短时偏差。
- 新增电池供电明细与 7/30/90/180/365 天统计，默认 90 天；显示起止、观测时长、起止电量和净下降，区分完整、不完整与进行中记录。
- 供电明细从升级后的首次有效样本开始记录，不从旧事件或聚合历史补造。重复/倒序样本不增加次数，采集中断不算作持续供电。
- 次数只统计窗口内确认的开始，观测时长按窗口裁剪；总电量净下降只汇总完整、全部落在窗口内且起止电量齐全的记录，单位为百分点。原状态事件继续保留。
- **只需更新 Docker 面板，v0.4.0 宿主机采集器继续兼容。** 原有校准配置与 NAS UPS 保护保持原用途。

## 0.4.1 — 简化系数显示（2026-09-06）

- 功率校准页的当前系数与表单默认最多显示 4 位小数，去除冗长尾数。
- 计算和未修改字段的保存继续使用完整精度；极小非零值以科学计数法显示。
- 已使用 v0.4.0 采集器的部署只需更新容器。

## 0.4.0 — 网页功率校准（2026-09-06）

### 功率校准

- 网页显示当前生效配置、三个精确系数与公式，支持 `none`、`local-19v-v1` 和 `custom`；默认仍为 `none`。
- 自定义 `base_gain`、`battery_gain` 必须大于 `0` 且不超过 `10`，`charge_gain` 允许 `0`–`10`；输入范围不作为精度证据，`custom` 始终未独立验证。
- 配置保存到已有 `./data/calibration.json`，由宿主机采集器动态读取，无需新增 Compose 参数。页面区分保存成功与采集器生效，仅新鲜遥测能确认后者。
- 配置变更后重建估算窗口，继续使用 18–20 V 交流输入限制与 8 秒平滑。原始量和已有历史保留，不同 `calibration_revision` 分开统计与导出。
- **旧用户升级需重装一次采集器。** 安装器新增 `--data-dir`，可从临时下载的 GitHub 源码安装并沿用现有数据目录；NAS 运行目录无需重新保留源码。
- 页面与 API 仍无内置认证，可访问服务的用户也能修改校准配置；UPS 控制和断电保护始终由 NAS 原服务负责。

### 部署与维护

- 整理项目文件：移除早期交付记录与专项采集工具，0.3.1 发布验证概要归入下方历史记录。
- 主 Compose 文件改名为 `docker-compose.yaml`，移除可选的顶层 `name`，项目名默认来自部署目录；开发覆盖文件仍为 `compose.build.yaml`。
- Compose 进一步精简为镜像、自动重启、端口和两处挂载，附用途注释；使用程序默认路径，移除多余环境变量和可选容器限制。

- 普通部署简化为安装宿主机采集器后运行 Docker Compose；安装脚本自动准备历史数据目录。
- Compose 直接提供 `latest` 镜像、`9086` 端口和持久目录，不再需要 `.env` 配置文件。
- 精简双语 README，移除旧版历史迁移流程与一次性迁移工具，开发和备份说明移至深入文档。
- 简化后续交付：首次克隆 GitHub 仓库安装采集器，面板使用 Docker Hub 镜像与 Docker Compose。
- GitHub Release 仅提供版本说明与镜像信息，不再额外制作源码或部署压缩包；已发布的历史附件保留。
- CI 直接验证仓库、Compose、宿主机脚本和镜像，保留公开文件检查。

## 0.3.1 — 预构建镜像与部署包

### 部署变化

- 默认 Compose 使用 Docker Hub 预构建镜像，普通 NAS 安装无需下载前端源码、安装 Node.js 或现场构建。
- 新增轻量部署包；宿主机采集器仍采用 systemd，只在首次安装或采集器变更时更新。
- Compose 项目名固定为 `ugreen-ups-panel`，历史改为默认绑定部署目录下的 `./data`；数据与容器生命周期分开。
- 提供旧版命名卷历史的显式迁移工具，迁移保留原数据库与原卷，不覆盖已有目标目录。
- 镜像构建目标包含 `linux/amd64` 与 `linux/arm64`；真实 UPS/NAS 硬件验证范围仍仅为 x86_64。

### 文档与开发

- 双语 README 区分首次安装、旧版数据迁移、镜像更新和源码开发。
- 文档明确开发电脑、NAS 部署目录、宿主机采集器及运行快照的用途。
- 源码构建使用独立的 `compose.build.yaml` 覆盖文件；默认 Compose 不再隐式构建。
- 数据库保持 schema 2，校准模型的默认值与适用边界不变。

### 发布验证

- 发布时 89 项自动化测试、TypeScript 检查与前端生产构建通过；当前测试集以 CI 为准。
- `linux/amd64` 与 `linux/arm64` 面板镜像通过启动、接口、历史记录与 CSV 检查，并发布至 Docker Hub；ARM NAS 的采集器与原 UPS 驱动组合尚未经实机验证。
- x86_64 NAS 完成镜像升级与 schema 2 历史迁移，数据库完整性、容器健康及实时采集检查通过；升级期间宿主机采集器与原 NUT 保护服务持续运行。

## 0.3.0 — 发布准备

### 兼容性变化

- 首次安装默认关闭单机功率模型；`local-19v-v1` 需要显式选择。已有宿主机配置在正常升级中保留。
- 历史数据库升级到 schema 2。升级前应备份；回退到旧版时应恢复原数据库。

### 完善

- 统一公开文档与当前解码器字段解释，明确交流、电池、直流输出三个测点。
- 将候选电流、设备报告值与经验功率估算区分，避免把开发样机系数描述为通用仪器校准。
- 补充安装、演示、架构、硬件来源、验证范围和安全边界。
- 发布内容排除本地配置、运行数据库、专项采集档案及第三方拆解图片。
- 加入演示截图、应用图标与第三方运行依赖许可；CI 验证导出后的源码可测试、可构建。

## 0.2.x — 功率模型与硬件资料

- 分开适配器输入、UPS 输出与电池组电压，改用明确测点的历史指标。
- 增加充放电电流/功率候选，保留未经确认的原始字段。
- 增加 19 V 接线下的交流输入经验模型、回充补偿和 8 秒平滑。
- 增加基于标称容量与电量趋势的电池供电估算，独立记录历史。
- 增加数据说明及硬件档案；上述模型来自单机研究，未验证为通用精度。

## 0.1.x — 初始实现

- 被动 usbmon 读取、原子快照、NUT 只读查询。
- React 页面、FastAPI、SQLite 聚合与事件、CSV。
- Docker 与 systemd 部署、安装验证和采集器回退。
